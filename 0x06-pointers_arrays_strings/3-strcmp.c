#include "main.h"
#include <string.h>

/**
* _strcmp - compare two things like s1 and s2
* @s1: first arg
* @s2: second arg
* Return: integer
*/

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);

	return (result);
}
